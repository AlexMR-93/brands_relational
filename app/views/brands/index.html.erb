<h1>Brands</h1>

<ul>
  <% @brands.each do |brand| %>
    <li>
      <a href="/brands/<%= brand.id %>">
        <%= brand.name %>
      <%= brand.num_of_models %>
      </a>
    </li>
  <% end %>
</ul>
